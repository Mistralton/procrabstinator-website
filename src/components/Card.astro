---
export interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<div class="p-4 text-left border border-orange-400 shadow">
	<a href={href}>
		<h2 class="mb-4">
			{title}
		</h2>
		<p>
			{body}
		</p>
	</a>
</div>